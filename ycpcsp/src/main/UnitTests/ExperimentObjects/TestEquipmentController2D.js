QUnit.todo('EquipmentController2D constructor:', function(assert){
    assert.true(false);
});

QUnit.todo('EquipmentController2D draw:', function(assert){
    assert.true(false);
});

QUnit.todo('EquipmentController2D setEquipment:', function(assert){
    assert.true(false);
});

QUnit.todo('EquipmentController2D inBounds:', function(assert){
    assert.true(false);
});