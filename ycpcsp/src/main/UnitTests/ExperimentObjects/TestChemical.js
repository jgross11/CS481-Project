QUnit.todo('Chemical constructor:', function(assert){
    assert.true(false);
});

QUnit.todo('Chemical setEquation:', function(assert){
    assert.true(false);
});

QUnit.todo('Chemical setTemperature:', function(assert){
    assert.true(false);
});

QUnit.todo('Chemical setTexture:', function(assert){
    assert.true(false);
});